package Enums;

public class Enums {
    public enum State {
        STMT,
        LIST,
        EXPR
    }

    public enum Token {
        IF,
        NOT,
        EOF,
        ELSE,
        TRUE,
        FALSE,
        WHILE,
        LBRAC,
        RBRAC,
        LPAREN,
        RPAREN,
        SYSOUT,
        SEMICOLON
    }
}
