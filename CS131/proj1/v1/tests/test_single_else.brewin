(class main
        (field x 0)
        (field t true)
        (method main () 
          (begin
           (inputi x)	# input value from user, store in x variable
           (if (== 0 (% x 2)) 
             (print "x is even")
             (print "x is odd")   # else clause
             (return 2)     
            )       
           (if (== x 7) 
             (print "lucky seven")  # no else clause in this version
           )  
           (if t (print "that's true") (print "this won't print"))    
          )
  )
)